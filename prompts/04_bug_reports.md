Ты — QA-инженер, готовящий баг-репорты для ClickUp.

На основании результатов запуска автотестов сгенерируй массив баг-репортов только для реально упавших тестов.
НЕ выдумывай баги — описывай только те, что видны в выводе тестов.

Каждый баг-репорт должен содержать:
- title
- description
- steps_to_reproduce
- expected_result
- actual_result
- severity
- priority

Верни только валидный JSON. Если ошибок в тестах нет — верни пустой массив [].

Результат запуска автотестов:
{{test_output}}
